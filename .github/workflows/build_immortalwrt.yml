name: 🚀 编译 ImmortalWrt for SL-3000 (无USB版) - 优化版

on:
  workflow_dispatch:
    inputs:
      skip_kmods:
        description: "跳过内核模块编译（节省空间）"
        type: boolean
        default: true
      minimal_build:
        description: "最小化编译（仅核心+Docker）"
        type: boolean
        default: true
      clean_build:
        description: "完全清理构建"
        type: boolean
        default: false

env:
  REPO_URL: "https://github.com/immortalwrt/immortalwrt"
  BRANCH: "openwrt-24.10"
  TZ: "Asia/Shanghai"

jobs:
  build:
    runs-on: ubuntu-22.04
    name: 🚀 编译 SL-3000 固件 (无USB版)
    timeout-minutes: 180
    
    steps:
      - name: 初始磁盘空间检查
        run: |
          echo "=== 初始磁盘空间 ==="
          df -h
          echo "==================="

      - name: 深度磁盘清理
        run: |
          echo "执行深度磁盘清理..."
          sudo apt autoremove -y
          sudo apt autoclean
          sudo apt clean
          sudo rm -rf /var/lib/apt/lists/*
          sudo rm -rf /usr/local/lib/android /opt/hostedtoolcache/CodeQL /usr/share/dotnet 2>/dev/null || true
          sudo rm -rf /opt/ghc /opt/hostedtoolcache/PyPy /opt/microsoft 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          sudo rm -rf /var/lib/docker/* 2>/dev/null || true
          sudo rm -rf /tmp/* /var/tmp/* /var/cache/* ~/.cache/*
          sudo journalctl --vacuum-time=1h 2>/dev/null || true
          sudo find /var/log -type f -name "*.log" -exec truncate -s 0 {} \; 2>/dev/null || true
          echo "清理完成"

      - name: 安装核心依赖（最小化）
        run: |
          echo "安装核心编译依赖..."
          sudo apt update
          
          # 只安装最核心的编译工具
          sudo apt install -y --no-install-recommends \
            build-essential ccache curl file gawk gettext git \
            libncurses-dev libssl-dev python3 python3-pip python3-setuptools \
            rsync unzip wget zlib1g-dev \
            gcc-multilib g++-multilib pkg-config autoconf automake libtool \
            device-tree-compiler lld llvm ninja-build \
            gperf help2man texinfo xsltproc \
            asciidoc bison bzip2 diffutils findutils \
            gzip make patch perl re2c scons squashfs-tools time xxd
          
          # 清理APT缓存
          sudo apt clean
          sudo rm -rf /var/lib/apt/lists/*
          echo "核心依赖安装完成"

      - name: 设置编译优化
        run: |
          # 设置ccache
          ccache -M 2G 2>/dev/null || true
          # 优化git设置减少内存使用
          git config --global pack.windowMemory 50m
          git config --global pack.packSizeLimit 50m
          git config --global core.compression 9
          git config --global core.packedGitLimit 128m
          git config --global core.packedGitWindowSize 16m
          # 设置ulimit避免内存不足
          ulimit -s 32768 2>/dev/null || true

      - name: 克隆当前仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 浅克隆 ImmortalWrt 源码
        uses: actions/checkout@v4
        with:
          repository: immortalwrt/immortalwrt
          ref: ${{ env.BRANCH }}
          path: openwrt
          fetch-depth: 1

      - name: 验证源码目录
        run: |
          echo "=== 验证 openwrt 目录 ==="
          if [ -d "openwrt" ]; then
            echo "✅ openwrt 目录存在"
            ls -la openwrt/
          else
            echo "❌ openwrt 目录不存在，尝试完整克隆"
            git clone --depth=1 $REPO_URL -b $BRANCH openwrt
          fi

      - name: 编译前空间确认
        run: |
          echo "=== 开始编译前磁盘空间 ==="
          df -h
          AVAILABLE=$(df / --output=avail | tail -1)
          echo "可用空间: ${AVAILABLE}KB"
          if [ $AVAILABLE -lt 10000000 ]; then
            echo "⚠️ 磁盘空间紧张，启用极简编译模式"
          else
            echo "✅ 磁盘空间充足"
          fi

      - name: 应用无USB配置
        run: |
          echo "应用无USB配置..."
          mkdir -p openwrt
          # 使用逐行写入避免多行字符串问题
          echo "CONFIG_TARGET_mediatek=y" > openwrt/.config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> openwrt/.config
          echo "CONFIG_TARGET_mediatek_filogic_DEVICE_sl_3000-emmc=y" >> openwrt/.config
          echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y" >> openwrt/.config
          echo "CONFIG_TARGET_ROOTFS_EXT4FS=n" >> openwrt/.config
          echo "CONFIG_TARGET_KERNEL_PARTSIZE=32" >> openwrt/.config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=1024" >> openwrt/.config
          echo "CONFIG_CCACHE=y" >> openwrt/.config
          echo "CONFIG_BUILD_LOG=y" >> openwrt/.config
          echo "CONFIG_SINGLE_IMAGE=y" >> openwrt/.config
          # 核心包
          echo "CONFIG_PACKAGE_block-mount=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_uhttpd=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_fstools=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_procd=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_uci=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_ubus=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_ubox=y" >> openwrt/.config
          # 网络驱动
          echo "CONFIG_PACKAGE_kmod-mt7915e=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-mt7916-firmware=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-mtk-esw-rt3050=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-mtk-hnat=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-mtk-ppe=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-phy-mediatek-ge-soc=y" >> openwrt/.config
          # 网络服务
          echo "CONFIG_PACKAGE_dnsmasq-full=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_firewall=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_odhcpd=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_odhcp6c=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_iptables=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_ip6tables=y" >> openwrt/.config
          # Docker相关
          echo "CONFIG_PACKAGE_docker=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_dockerd=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_libdevmapper=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_libltdl=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-dm=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-br-netfilter=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-veth=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-ipt-core=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-ipt-nat=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-nf-conntrack=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-nf-nat=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-fuse=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-loop=y" >> openwrt/.config
          # 文件系统
          echo "CONFIG_PACKAGE_kmod-fs-ext4=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_kmod-fs-squashfs=y" >> openwrt/.config
          # Luci
          echo "CONFIG_PACKAGE_luci=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_luci-ssl-openssl=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_luci-app-dockerman=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_luci-app-firewall=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_luci-theme-bootstrap=y" >> openwrt/.config
          # 工具
          echo "CONFIG_PACKAGE_curl=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_bash=y" >> openwrt/.config
          echo "CONFIG_PACKAGE_python3=y" >> openwrt/.config
          echo "配置文件已生成"

      - name: 更新feeds（简化版）
        run: |
          echo "更新feeds..."
          cd openwrt
          # 只更新基础feeds，跳过部分可选包
          ./scripts/feeds update base packages luci
          ./scripts/feeds install -a
          echo "feeds更新完成"

      - name: 配置检查
        run: |
          echo "应用配置优化..."
          cd openwrt
          make defconfig
          ./scripts/diffconfig.sh > diffconfig.txt
          echo "=== 最终配置 ==="
          cat diffconfig.txt
          echo "配置优化完成"

      - name: 下载源码（带重试）
        run: |
          echo "下载源码（带重试机制）..."
          cd openwrt
          for i in {1..3}; do
            echo "第 $i 次尝试下载..."
            if make download -j2 V=0; then
              echo "✅ 下载成功"
              break
            else
              echo "❌ 第 $i 次下载失败"
              if [ $i -eq 3 ]; then
                echo "⚠️ 最终下载失败，尝试单线程下载..."
                make download -j1 V=0 || true
              fi
              sleep 10
            fi
          done

      - name: 编译工具链（分步进行）
        run: |
          echo "编译工具链（分步进行）..."
          cd openwrt
          # 分步编译工具链，避免资源竞争
          echo "步骤1: 编译基础工具..."
          make tools/install -j1 V=0 || make tools/install -j1 V=s
          
          echo "步骤2: 编译工具链..."
          make toolchain/install -j1 V=0 || make toolchain/install -j1 V=s
          
          echo "✅ 工具链编译完成"

      - name: 编译核心系统（优化版）
        run: |
          echo "编译核心系统（优化版）..."
          cd openwrt
          
          # 设置编译环境变量优化
          export MAKE_ARGS="-j2"
          export GLUON_DEPRECATED=1
          
          if [ "${{ github.event.inputs.minimal_build }}" = "true" ]; then
            echo "使用最小化编译模式..."
            # 分步编译，避免卡住
            echo "1. 编译内核..."
            make target/linux/install $MAKE_ARGS V=0
            
            echo "2. 编译基础包..."
            make package/base-files/install $MAKE_ARGS V=0
            make package/kernel/linux/install $MAKE_ARGS V=0
            
            echo "3. 编译系统核心..."
            make package/system/opkg/install $MAKE_ARGS V=0
            make package/system/procd/install $MAKE_ARGS V=0
            
            echo "4. 编译网络核心..."
            make package/network/services/dnsmasq/install $MAKE_ARGS V=0
            make package/network/config/firewall/install $MAKE_ARGS V=0
            
            echo "5. 最终编译..."
            make $MAKE_ARGS V=0 || make -j1 V=s
          else
            echo "使用标准编译模式..."
            make $MAKE_ARGS V=0 || make -j1 V=s
          fi

      - name: 编译Docker组件（条件编译）
        if: ${{ !github.event.inputs.skip_kmods }}
        run: |
          echo "编译Docker组件..."
          cd openwrt
          # 单独编译Docker相关包，使用更稳健的方法
          echo "开始编译Docker相关组件..."
          
          # 检查Docker包是否存在
          if [ -d "package/feeds/packages/docker" ] || [ -d "feeds/packages/utils/docker" ]; then
            echo "编译Docker核心包..."
            make package/feeds/packages/docker/compile -j1 V=0 || \
            make package/feeds/packages/docker/compile -j1 V=s
          else
            echo "Docker包目录不存在，尝试其他路径..."
            find feeds -name "docker" -type d | head -5
          fi
          
          if [ -d "package/feeds/packages/dockerd" ] || [ -d "feeds/packages/utils/dockerd" ]; then
            echo "编译Docker守护进程..."
            make package/feeds/packages/dockerd/compile -j1 V=0 || \
            make package/feeds/packages/dockerd/compile -j1 V=s
          fi
          
          if [ -d "package/feeds/packages/containerd" ]; then
            echo "编译containerd..."
            make package/feeds/packages/containerd/compile -j1 V=0
          fi
          
          echo "Docker组件编译完成"

      - name: 空间清理（编译后）
        run: |
          echo "编译后清理临时文件..."
          cd openwrt
          # 清理编译中间文件，保留最终产物
          if [ -d "build_dir" ]; then
            find build_dir -name "*.o" -delete 2>/dev/null || true
            find build_dir -name "*.a" -delete 2>/dev/null || true
            find build_dir -name "*.lo" -delete 2>/dev/null || true
          fi
          echo "清理完成"

      - name: 验证编译结果
        run: |
          echo "检查编译产物..."
          if [ -d "openwrt" ]; then
            cd openwrt
            if find bin/targets -name "*sl_3000*" -o -name "*sl3000*" 2>/dev/null | grep -q .; then
              echo "✅ SL-3000固件编译成功"
              find bin/targets -name "*.bin" -exec echo "固件: {}" \;
              # 显示文件大小
              find bin/targets -name "*.bin" -exec ls -lh {} \;
            else
              echo "❌ 未找到SL-3000固件"
              echo "当前目录内容:"
              ls -la bin/targets/ 2>/dev/null || echo "bin/targets目录不存在"
              # 检查是否有其他产物
              find bin -name "*.bin" -o -name "*.img" 2>/dev/null | head -10
              exit 1
            fi
          else
            echo "❌ openwrt目录不存在"
            exit 1
          fi

      - name: 上传编译产物
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-sl3000-nousb-${{ github.run_id }}
          path: |
            openwrt/bin/targets/**/*.bin
            openwrt/bin/targets/**/*.manifest
            openwrt/bin/packages/**/docker*.ipk
          retention-days: 30
          compression-level: 0

      - name: 生成编译报告
        if: always()
        run: |
          echo "=== 编译完成报告 ===" > build-report.txt
          echo "时间: $(date)" >> build-report.txt
          echo "工作流结果: ${{ job.status }}" >> build-report.txt
          echo "最终磁盘空间:" >> build-report.txt
          df -h >> build-report.txt
          echo "编译配置:" >> build-report.txt
          echo "  - 最小化编译: ${{ github.event.inputs.minimal_build }}" >> build-report.txt
          echo "  - 跳过内核模块: ${{ github.event.inputs.skip_kmods }}" >> build-report.txt
          
          if [ -d "openwrt" ]; then
            echo "编译产物:" >> build-report.txt
            find openwrt/bin -name "*.bin" -exec echo "固件: {}" \; >> build-report.txt 2>/dev/null || echo "未找到固件文件" >> build-report.txt
          else
            echo "openwrt目录不存在" >> build-report.txt
          fi
          
          cat build-report.txt
